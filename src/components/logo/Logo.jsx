import React from 'react';
import logo from '../../assets/logo.png';

const Logo = () => {
    return (
        <div>
            <img width={130} src={logo}></img>
        </div>
    );
};

export default Logo;